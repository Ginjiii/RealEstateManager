<resources>

        <!-- Base application theme. -->
        <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
            <!-- Customize your theme here. -->
            <item name="colorPrimary">@color/colorPrimary</item>
            <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
            <item name="colorAccent">@color/colorAccent</item>
            <item name="colorOnPrimary">@color/colorTextAccent</item>
        </style>

        <style name="splashScreenTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
            <item name="android:windowBackground">@drawable/immo3</item>
        </style>

        <style name="CustomChipStyle" parent="Widget.MaterialComponents.Chip.Filter">
            <item name="chipBackgroundColor">@color/custom_chip_background</item>
        </style>

        <style name="CustomInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
            <item name="boxStrokeColor">@color/custom_input_layout_outlined</item>
            <item name="boxStrokeWidth">2dp</item>
            <item name="hintTextColor">@android:color/black</item>
        </style>

        <style name="Divider">
            <item name="android:background">@color/colorAccent</item>
        </style>

        <style name="DividerHorizontal" parent="Divider">
            <item name="android:layout_width">match_parent</item>
            <item name="android:layout_height">2dp</item>
        </style>

        <style name="DividerVertical" parent="Divider">
            <item name="android:layout_width">3dp</item>
            <item name="android:layout_height">0dp</item>
        </style>

    <!-- TOOLBAR THEME -->
    <style name="ToolBarTheme" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/colorTextAccent</item>
    </style>

    <!-- TOOLBAR TITLE THEME -->
    <style name="ToolBarTheme.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">@dimen/toolbar_text_size</item>
    </style>

    <style name="TitleREM">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="SmallTextREM">
        <item name="android:textSize">20sp</item>
    </style>

    <style name="SmallTitleREM" parent="SmallTextREM">
        <item name="android:textStyle">bold</item>

    </style>

    <style name="IconDetailREM">
        <item name="android:layout_weight">@dimen/icon_details_size</item>
        <item name="android:layout_height">@dimen/icon_details_size</item>
        <item name="android:tint">@color/colorPrimaryDark</item>
    </style>

    <style name="ButtonOutlineREM" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="strokeWidth">2dp</item>
        <item name="strokeColor">?attr/colorSecondary</item>
        <item name="android:textColor">?attr/colorSecondary</item>
        <item name="iconTint">?attr/colorSecondary</item>
        <item name="rippleColor">?attr/colorOnSecondary</item>
    </style>

</resources>